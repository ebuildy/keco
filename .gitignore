node_modules/
.pnpm-store/

# build / cache artifacts
dist/
*.tsbuildinfo
coverage/
.turbo/

# local write model (fs cache adapter) — disposable, never committed
.cache/
.data/

# MSW worker script — generated by `mise run web:mock`. Kept out of a clean checkout, CI and
# deployments; `mise run build` drops any local copy, and assert:no-mocks is the backstop (§9).
apps/web/public/mockServiceWorker.js

# Playwright run artifacts — reports, traces and screenshots from `mise run e2e`
apps/web/playwright-report/
apps/web/test-results/
apps/web/blob-report/

# env
.env
.env.*
!.env.example

# editors / os
.DS_Store
*.pem
*.log

# Superpowers brainstorming session artifacts
.superpowers/

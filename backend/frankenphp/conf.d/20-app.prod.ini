; Production: application code doesn't change without a redeploy (frankenphp_prod bakes it into
; the image, AGENTS.md §7's "two deployables"), so skip Opcache's per-request mtime check — the
; dev counterpart (20-app.dev.ini) leaves this at Symfony's dev-friendly default (on).
opcache.validate_timestamps = 0
